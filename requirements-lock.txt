# DEPRECATED: This file is kept for backward compatibility.
# The real lock file is requirements-lock-core.txt (generated from pip freeze).
#
# See also:
#   requirements-core.txt      — core deps with version ranges
#   requirements-dev.txt       — lint/test tooling
#   requirements-ml-extra.txt  — optional torch/HF/diffusion stack
#   requirements-lock-core.txt — reproducible lock (actual installed versions)
#
# To install the locked core environment:
#   pip install -r requirements-lock-core.txt
#
# To install with ranges (for development):
#   pip install -r requirements-core.txt
#
-r requirements-lock-core.txt
